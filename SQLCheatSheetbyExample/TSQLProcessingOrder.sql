--Order of Processing a T-SQL Statement
/*
FROM        -- Source
WHERE       -- Filter
GROUP BY    -- Aggregation
HAVING      -- CHECK against aggregation
SELECT      -- Data Columns selection
ORDER BY    -- Ordering Data -- Expensive
*/
